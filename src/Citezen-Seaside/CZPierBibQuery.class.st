"
I represent a fixed query on a [CZPier]BibFile. I cache my result (a BibList), and update it only if it expires.

I have a fixed URL, so am a PRStructure.
"
Class {
	#name : #CZPierBibQuery,
	#superclass : #PRStructure,
	#category : #'Citezen-Seaside'
}
